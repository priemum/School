<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('../partials/head.ejs') %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="stylesheet" href="/public/css/forms.css" />
    <link rel="stylesheet" href="/css/add-teacher.css" />
  
  </head>
<body>
    <!-- Header -->    
    <%- include('../partials/header.ejs') %>
    <!-- Header Ends!! -->
    <div class="center-container">
      <% if (messages.success) { %>
          <div class="alert alert-success"><%= messages.success %></div>
      <% } %>
      <% if (messages.error) { %>
          <div class="alert alert-danger"><%= messages.error %></div>
      <% } %>
      <div>
        <h3 id="h4">اضافة الوحدة</h3>
      </div>
     <a href="/Modules" title="إلفاء" id="cancel" class="mt-5"><button type="button" class="btn-close" aria-label="Close" style="margin-left: 13px;"></button></a> 
     <div class="container my-5" dir="rtl">
          <form method="POST"  action="/edit-Module/<%= ModuleInfo._id %>?_method=PUT" class="row g-3" autocomplete="off">
            <div class="col-md-6">
              <label class="form-label cus-label" >الوحدة</label><span> :</span>
              <input type="text"  value="<%= ModuleInfo.name_module %>" name="name_module" required placeholder="اسم المادة" id="input-field" class="form-control">
            </div>
            <div class="col-md-6">
              <label class="form-label cus-label" > وصف الوحدة   </label><span> :</span>
              <input type="text" value="<%= ModuleInfo.desc_module %>" name="desc_module" placeholder="وصف للمادة" id="input-field" class="form-control">
            </div>
            <div class="col-md-6">
              <label class="form-label cus-label" >الفصل</label><span> :</span>
              <select class="form-select" value="<%= ModuleInfo.selected_semester %>" aria-label="Default select example" id="input-field" name="selected_semester">
                <option value="S1">S1</option>
                <option value="S2">S2</option>
                <option value="S3">S3</option>
                <option value="S4">S4</option>
                <option value="S5">S5</option>
                <option value="S6">S6</option>
              </select>
            </div>
            <div class="custom-control custom-checkbox">
                <input type="checkbox" class="custom-control-input" id="customCheck1" required>
                <label class="custom-control-label" for="customCheck1">اشهد على صحة المعلومات المصرح بها</label>
            </div>
            <button type="submit" class="btn w-25 btn-primary">إضافة</button>
          </form>
      </div>
    </div>
    <!-- <script>
            const passwordInput = document.querySelector('input[name="password"]');
            const confirmInput = document.querySelector('input[name="confirm_password"]');
            const togglePassword = document.getElementById('togglePassword');
            
            togglePassword.addEventListener('click', function () {
              togglePasswordVisibility(passwordInput);
              togglePasswordVisibility(confirmInput);
            });
            
            function togglePasswordVisibility(inputField) {
              if (inputField.type === 'password') {
                    inputField.type = 'text';
                } else {
                    inputField.type = 'password';
                  }
            }
    </script> -->
    
    <!-- footer --->
        <%- include('../partials/footer.ejs') %>
    <!-- footer ends -->
</body>
</html>
